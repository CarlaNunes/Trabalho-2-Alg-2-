//Versão para coreção

#include<stdio.h>

#include "grafo.h"


char Fim_Vertice(tgrafo *grafo,taresta aresta, int num_vertice){
    tvertice vertice;
    if((grafo->num_vertices =  num_vertice) ){
       for(int i; i < 2;i++){
            return vertice->nomeCompleto;
       }
    }
    else{
        return NULL;
    }
}
